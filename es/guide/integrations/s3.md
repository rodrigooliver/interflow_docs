# Integración AWS S3

Use su propio bucket S3 para archivos de la organización.

1. Cree bucket + credenciales IAM (mínimo privilegio)
2. **Configuración → Integraciones → AWS S3**
3. Informe región/endpoint, bucket y claves → Guardar
