{
  "name": "interflow-docs",
  "version": "1.0.0",
  "description": "Documentação oficial da Interflow",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-i18n.js",
    "dev": "npm run sync && vitepress dev",
    "build": "npm run sync && vitepress build",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "vitepress": "^1.5.0",
    "vue": "^3.5.13"
  }
}
