import { ssrRenderAttrs, ssrInterpolate } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Node: Webhook Trigger","description":"","frontmatter":{},"headers":[],"relativePath":"en/guide/flows/nodes/webhook-trigger.md","filePath":"en/guide/flows/nodes/webhook-trigger.md","lastUpdated":1779486658000}');
const _sfc_main = { name: "en/guide/flows/nodes/webhook-trigger.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="node-webhook-trigger" tabindex="-1">Node: Webhook Trigger <a class="header-anchor" href="#node-webhook-trigger" aria-label="Permalink to &quot;Node: Webhook Trigger&quot;">​</a></h1><p>Receives events from external systems and automatically starts a flow.</p><h2 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h2><p>The <strong>Webhook Trigger</strong> node is the entry point for a flow activated by an external system via HTTP. Unlike the Start node (which requires user interaction), the Webhook Trigger is activated by an API call — from sales platforms, ERPs, CRMs or any service that sends data.</p><p>Use cases:</p><ul><li>Create or update a customer when a new order arrives from a store</li><li>Start a welcome flow when a lead registers</li><li>Route different actions based on the received event type</li><li>Integrate with services like <strong>Kiwify</strong>, <strong>Hotmart</strong>, <strong>Shopify</strong> or any custom webhook</li></ul><h2 id="how-to-add-to-a-flow" tabindex="-1">How to add to a flow <a class="header-anchor" href="#how-to-add-to-a-flow" aria-label="Permalink to &quot;How to add to a flow&quot;">​</a></h2><ol><li>In the flow builder, drag the <strong>Webhook Trigger</strong> node onto the canvas</li><li>Click the node to open the configuration panel</li><li>A unique URL will be generated automatically — copy it and set it up in the external system</li></ol><h2 id="configuration-tabs" tabindex="-1">Configuration tabs <a class="header-anchor" href="#configuration-tabs" aria-label="Permalink to &quot;Configuration tabs&quot;">​</a></h2><h3 id="tab-general" tabindex="-1">Tab: General <a class="header-anchor" href="#tab-general" aria-label="Permalink to &quot;Tab: General&quot;">​</a></h3><h4 id="webhook-url" tabindex="-1">Webhook URL <a class="header-anchor" href="#webhook-url" aria-label="Permalink to &quot;Webhook URL&quot;">​</a></h4><p>The URL is automatically generated in the format:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>https://api.yourapp.com/webhooks/flow/{id}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Copy this URL and configure it in the external system as the webhook destination.</p><h4 id="variable-mappings" tabindex="-1">Variable Mappings <a class="header-anchor" href="#variable-mappings" aria-label="Permalink to &quot;Variable Mappings&quot;">​</a></h4><p>Map payload fields to flow variables:</p><table tabindex="0"><thead><tr><th>Body Field</th><th>Flow Variable</th></tr></thead><tbody><tr><td><code>body.user.name</code></td><td><code>customer_name</code></td></tr><tr><td><code>body.order.total</code></td><td><code>order_amount</code></td></tr><tr><td><code>body.event</code></td><td><code>event_type</code></td></tr></tbody></table><p>Use dot notation for nested fields and <code>[0]</code> for arrays:</p><ul><li><code>body.data[0].email</code></li><li><code>body.customer.address.city</code></li></ul><h4 id="save-full-body-as-json" tabindex="-1">Save full body as JSON <a class="header-anchor" href="#save-full-body-as-json" aria-label="Permalink to &quot;Save full body as JSON&quot;">​</a></h4><p>Enable this option to save the entire payload to an <code>object</code> type variable. Useful for dynamic payloads or for inspecting the full content.</p><hr><h3 id="tab-security" tabindex="-1">Tab: Security <a class="header-anchor" href="#tab-security" aria-label="Permalink to &quot;Tab: Security&quot;">​</a></h3><h4 id="authentication-type" tabindex="-1">Authentication Type <a class="header-anchor" href="#authentication-type" aria-label="Permalink to &quot;Authentication Type&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><strong>Bearer</strong></td><td>Token in the <code>Authorization: Bearer &lt;token&gt;</code> header</td></tr><tr><td><strong>Custom header</strong></td><td>Token in a user-defined header name</td></tr><tr><td><strong>Query param</strong></td><td>Token as a URL parameter (<code>?token=xxx</code>)</td></tr><tr><td><strong>HMAC</strong></td><td>Cryptographic signature in the payload (see below)</td></tr><tr><td><strong>Public</strong></td><td>No authentication — accepts any call</td></tr></tbody></table><p><strong>Managing the token:</strong></p><ul><li>Click <strong>Generate new token</strong> to create a secure token automatically</li><li>Or enter your own value in the text field and click <strong>Save token</strong></li><li>The token is stored <strong>encrypted</strong> in the database</li><li>Click the eye icon to reveal the original token (admins only)</li></ul><h4 id="hmac-authentication" tabindex="-1">HMAC Authentication <a class="header-anchor" href="#hmac-authentication" aria-label="Permalink to &quot;HMAC Authentication&quot;">​</a></h4><p>Use when the external system sends a <strong>cryptographic signature</strong> of the payload (e.g. Kiwify, Hotmart):</p><table tabindex="0"><thead><tr><th>Field</th><th>Description</th></tr></thead><tbody><tr><td><strong>Algorithm</strong></td><td>SHA-1, SHA-256 or SHA-512</td></tr><tr><td><strong>Signature source</strong></td><td><code>query</code> (URL parameter) or <code>header</code></td></tr><tr><td><strong>Parameter name</strong></td><td>Name of the query param or header containing the signature</td></tr><tr><td><strong>Secret</strong></td><td>The shared secret key with the external service</td></tr></tbody></table><p>Example for Kiwify:</p><ul><li>Algorithm: <code>SHA-1</code></li><li>Source: <code>query</code></li><li>Parameter: <code>signature</code></li></ul><h4 id="ip-allowlist" tabindex="-1">IP Allowlist <a class="header-anchor" href="#ip-allowlist" aria-label="Permalink to &quot;IP Allowlist&quot;">​</a></h4><p>List the allowed IPs (one per line). Calls from other IPs will be rejected with <code>403</code>. Leave empty to accept any IP.</p><h4 id="origin-allowlist" tabindex="-1">Origin Allowlist <a class="header-anchor" href="#origin-allowlist" aria-label="Permalink to &quot;Origin Allowlist&quot;">​</a></h4><p>List the allowed domains. E.g. <code>https://app.kiwify.com.br</code>. Leave empty to accept any origin.</p><hr><h3 id="tab-customer-chat" tabindex="-1">Tab: Customer &amp; Chat <a class="header-anchor" href="#tab-customer-chat" aria-label="Permalink to &quot;Tab: Customer &amp; Chat&quot;">​</a></h3><p>Configure automatic creation/update of customers and chats based on webhook data.</p><h4 id="customer-binding" tabindex="-1">Customer Binding <a class="header-anchor" href="#customer-binding" aria-label="Permalink to &quot;Customer Binding&quot;">​</a></h4><p>Enable the toggle and add customer fields:</p><ol><li>Click <strong>Add</strong> to add a new mapping row</li><li>Select the <strong>customer field</strong> (Name, Email, Document, WhatsApp, etc.)</li><li>Enter the <strong>body path</strong> (e.g. <code>body.customer.email</code>)</li><li>Click the <strong>search icon</strong> 🔍 on fields used to <strong>find</strong> the customer</li></ol><p><strong>Search logic (OR):</strong> the system looks for any customer matching <em>at least one</em> of the fields with the search icon active. Enable the search icon on at least one field.</p><p><strong>Update rules when a customer is found:</strong></p><table tabindex="0"><thead><tr><th>Field</th><th>Rule</th></tr></thead><tbody><tr><td><strong>Name</strong></td><td>Always updated</td></tr><tr><td><strong>Email / Phone</strong></td><td>Updates the direct column if empty; otherwise adds to <code>customer_contacts</code></td></tr><tr><td><strong>Document</strong></td><td>Updated only if currently empty</td></tr><tr><td><strong>WhatsApp, Instagram, Telegram</strong></td><td>Always added to <code>customer_contacts</code> (without overwriting)</td></tr></tbody></table><p>If no customer is found, a new one is created automatically with the mapped data.</p><h4 id="chat-binding" tabindex="-1">Chat Binding <a class="header-anchor" href="#chat-binding" aria-label="Permalink to &quot;Chat Binding&quot;">​</a></h4><p>Enable the toggle and configure:</p><table tabindex="0"><thead><tr><th>Field</th><th>Description</th></tr></thead><tbody><tr><td><strong>Channel</strong></td><td>Channel to create the chat on (WhatsApp, email, Instagram, etc.)</td></tr><tr><td><strong>Contact path</strong></td><td>Where the number/email is in the body (e.g. <code>body.customer.phone</code>)</td></tr><tr><td><strong>Contact type</strong></td><td><code>whatsapp</code>, <code>phone</code>, <code>email</code>, <code>instagram</code>, etc.</td></tr></tbody></table><p>The chat will be found or created automatically. The contact is also registered in <code>customer_contacts</code> if it doesn&#39;t exist yet.</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>With Customer + Chat binding configured, the flow runs in <strong>full mode</strong> — able to send messages, wait for responses, etc. Without binding (or incomplete binding), the flow runs in <strong>headless mode</strong> (only nodes that don&#39;t depend on a chat are executed).</p></div><hr><h3 id="tab-events" tabindex="-1">Tab: Events <a class="header-anchor" href="#tab-events" aria-label="Permalink to &quot;Tab: Events&quot;">​</a></h3><p>Configure conditional routing based on a payload field.</p><h4 id="event-path" tabindex="-1">Event Path <a class="header-anchor" href="#event-path" aria-label="Permalink to &quot;Event Path&quot;">​</a></h4><p>Enter where the event type is in the payload (e.g. <code>body.event</code>, <code>body.type</code>, <code>body.trigger</code>).</p><h4 id="cases" tabindex="-1">Cases <a class="header-anchor" href="#cases" aria-label="Permalink to &quot;Cases&quot;">​</a></h4><p>Add a case for each expected event type:</p><table tabindex="0"><thead><tr><th>Field</th><th>Description</th></tr></thead><tbody><tr><td><strong>Label</strong></td><td>Descriptive name (e.g. &quot;Purchase approved&quot;)</td></tr><tr><td><strong>Value</strong></td><td>Exact event value (e.g. <code>purchase.approved</code>) — supports <code>${ssrInterpolate(_ctx.variable)}</code></td></tr></tbody></table><p>Each case generates an <strong>orange output handle</strong> on the node. A <strong>gray (else) handle</strong> is always created for unmapped events.</p><p><strong>Routing example:</strong></p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>body.event = &quot;purchase.approved&quot;  →  handle &quot;Purchase approved&quot;</span></span>
<span class="line"><span>body.event = &quot;purchase.refunded&quot;  →  handle &quot;Refund&quot;</span></span>
<span class="line"><span>body.event = (anything else)      →  handle &quot;else&quot;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><hr><h2 id="listen-mode-—-real-time-testing" tabindex="-1">&quot;Listen&quot; Mode — Real-Time Testing <a class="header-anchor" href="#listen-mode-—-real-time-testing" aria-label="Permalink to &quot;&quot;Listen&quot; Mode — Real-Time Testing&quot;">​</a></h2><p>The <strong>Listen</strong> mode waits for a real webhook to arrive and displays the data to make setup easier.</p><ol><li>Click <strong>Listen</strong> in the node panel</li><li>Trigger a test webhook from the external system</li><li>Data appears in real time: body, headers, query params</li><li>The caller&#39;s IP and origin are shown as allowlist suggestions</li><li>Click <strong>Map</strong> next to any field to automatically add it to the variable mappings</li><li>Click the <strong>copy</strong> icon to copy the field path (e.g. <code>body.data[0].name</code>)</li></ol><p>The <strong>Logs</strong> tab shows the history of recent incoming calls and also lets you map variables from previous calls.</p><hr><h2 id="behavior-in-the-flow" tabindex="-1">Behavior in the flow <a class="header-anchor" href="#behavior-in-the-flow" aria-label="Permalink to &quot;Behavior in the flow&quot;">​</a></h2><h3 id="with-customer-chat-binding-configured" tabindex="-1">With Customer + Chat binding configured <a class="header-anchor" href="#with-customer-chat-binding-configured" aria-label="Permalink to &quot;With Customer + Chat binding configured&quot;">​</a></h3><p>The flow runs in <strong>full mode</strong>:</p><ul><li>All nodes execute normally</li><li>The customer and chat are linked to the flow session</li><li>You can send messages, wait for responses, etc.</li></ul><h3 id="without-binding-or-incomplete-binding" tabindex="-1">Without binding (or incomplete binding) <a class="header-anchor" href="#without-binding-or-incomplete-binding" aria-label="Permalink to &quot;Without binding (or incomplete binding)&quot;">​</a></h3><p>The flow runs in <strong>headless mode</strong>:</p><ul><li>Nodes that depend on a chat (send text, wait for input, etc.) are <strong>skipped</strong></li><li>Independent nodes (Variable, Update Customer, HTTP Request, etc.) <strong>execute normally</strong></li></ul><h3 id="event-based-routing" tabindex="-1">Event-based routing <a class="header-anchor" href="#event-based-routing" aria-label="Permalink to &quot;Event-based routing&quot;">​</a></h3><p>When the <strong>Events</strong> tab is configured:</p><ol><li>The backend extracts the event field value from the payload</li><li>Compares it with the configured cases (interpolating variables if needed)</li><li>Routes to the matching handle</li><li>If no case matches, uses the <strong>else</strong> handle</li></ol><hr><h2 id="usage-examples" tabindex="-1">Usage Examples <a class="header-anchor" href="#usage-examples" aria-label="Permalink to &quot;Usage Examples&quot;">​</a></h2><h3 id="kiwify-integration-hmac-sha-1" tabindex="-1">Kiwify Integration (HMAC SHA-1) <a class="header-anchor" href="#kiwify-integration-hmac-sha-1" aria-label="Permalink to &quot;Kiwify Integration (HMAC SHA-1)&quot;">​</a></h3><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Authentication: HMAC</span></span>
<span class="line"><span>Algorithm: SHA-1</span></span>
<span class="line"><span>Source: query</span></span>
<span class="line"><span>Parameter: signature</span></span>
<span class="line"><span>Event path: body.event</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Cases:</span></span>
<span class="line"><span>  &quot;order_approved&quot;  →  Start onboarding</span></span>
<span class="line"><span>  &quot;order_refunded&quot;  →  Cancel access</span></span>
<span class="line"><span>  else              →  Log unknown event</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><h3 id="new-lead-from-a-form" tabindex="-1">New lead from a form <a class="header-anchor" href="#new-lead-from-a-form" aria-label="Permalink to &quot;New lead from a form&quot;">​</a></h3><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Mappings:</span></span>
<span class="line"><span>  body.name   → name</span></span>
<span class="line"><span>  body.email  → email</span></span>
<span class="line"><span>  body.phone  → phone</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Customer Binding:</span></span>
<span class="line"><span>  Email ← body.email  [🔍 search]</span></span>
<span class="line"><span>  Name  ← body.name</span></span>
<span class="line"><span>  WhatsApp ← body.phone</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Chat Binding:</span></span>
<span class="line"><span>  Channel: WhatsApp Official</span></span>
<span class="line"><span>  Contact: body.phone</span></span>
<span class="line"><span>  Type: whatsapp</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br></div></div><hr><h2 id="tips" tabindex="-1">Tips <a class="header-anchor" href="#tips" aria-label="Permalink to &quot;Tips&quot;">​</a></h2><ul><li>Use <strong>multiple search fields</strong> (search icon) to avoid creating duplicate customers when the external system may send an email in one event and a phone number in another</li><li><strong>Listen mode</strong> is the fastest way to map variables — no need to manually edit paths</li><li>Combine with the <strong>Condition</strong> node after the Webhook Trigger for more complex logic beyond simple event routing</li><li>For public webhooks (no token), use the <strong>IP allowlist</strong> to ensure only the external server can trigger the flow</li></ul><h2 id="next-steps" tabindex="-1">Next Steps <a class="header-anchor" href="#next-steps" aria-label="Permalink to &quot;Next Steps&quot;">​</a></h2><ul><li><a href="/en/guide/flows/builder">Flow Builder</a></li><li><a href="/en/guide/flows/nodes/">Node Types</a></li><li><a href="/en/guide/flows/variables">Variables</a></li><li><a href="/en/guide/flows/nodes/request">HTTP Request</a></li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("en/guide/flows/nodes/webhook-trigger.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const webhookTrigger = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  webhookTrigger as default
};
